<Page x:Class="PhotoViewer.GridPhotoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:PhotoViewer"
      xmlns:controls="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
      Title="GridPhotoPage">

	<Page.Resources>
		<local:ImgConverter x:Key="imgConverter" />
	</Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
			<Button Content="フォルダを開く" Click="open_file" Width="120" Height="30"/>
		</StackPanel>
		<ListBox x:Name="mylistview"
                 Grid.Row="1"
                 ItemsSource="{Binding}"
                 SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.CacheLengthUnit="Page"
                 VirtualizingPanel.CacheLength="10,10">
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
					<controls:VirtualizingWrapPanel/>
				</ItemsPanelTemplate>
			</ListBox.ItemsPanel>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Grid Width="200" Height="200">
						<Image Source="{Binding FilePath, Converter={StaticResource imgConverter}, IsAsync=True}"
							   Stretch="UniformToFill"
							   MouseLeftButtonUp="Image_MouseLeftButtonUp"/>
					</Grid>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</Page>